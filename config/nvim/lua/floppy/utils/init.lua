local mappings = require("floppy.utils.mappings")

local M = {
    map = mappings.map,
    nmap = mappings.nmap,
    vmap = mappings.vmap,
    xmap = mappings.xmap,
    unmap = mappings.unmap,
    nunmap = mappings.nunmap,
    lazy = mappings.lazy,
}

return M
