[core]
  editor = "nvim"
  pager = "nvim -R"
  # pager = "(f=$(mktemp); cat > $f; $EDITOR $f; rm -v $f)"

[alias]
  st = status
  co = checkout
  ci = commit
  df = diff --no-color
  p  = push

[branch "trunk"]
    allowCommit = false
